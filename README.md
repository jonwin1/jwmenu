# jwmenu

## Configuration

Each menu is represented by a configuration file that defines the menu
structure. A menu consists of a list of items that either contain a command to
execute or a submenu. See an example menu in [example.toml](example.toml).

## TODO

- config file
  - dmenu command setting
  - submenu indicator setting
- code cleanup
- documentation
- and more
